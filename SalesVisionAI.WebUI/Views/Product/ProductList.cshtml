@model List<Product>
@{
    ViewData["Title"] = "ProductList";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<section class="content">
    <div class="block-header">
        <div class="row align-items-center">
            <div class="col-lg-7 col-md-6 col-sm-12">
                <h2>
                    <i class="zmdi zmdi-collection-item"></i> Ürün Listesi
                    <small class="text-muted d-block mt-2">Ürün İşlemleri Sayfası</small>
                </h2>
            </div>
            <div class="col-lg-5 col-md-6 col-sm-12">
                <ul class="breadcrumb float-md-right">
                    <li class="breadcrumb-item"><a href="/Dashboard/Index"><i class="zmdi zmdi-home"></i> Ana Sayfa</a></li>
                    <li class="breadcrumb-item"><a href="javascript:void(0);">Ürünler</a></li>
                    <li class="breadcrumb-item active">Ürün Listesi</li>
                </ul>
            </div>
        </div>
    </div>
    <div class="container-fluid">
        <div class="row clearfix">
            <div class="col-lg-12">
                <div class="card">
                    <div class="body">
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <div>
                                <h5 class="mb-0"><i class="zmdi zmdi-collection-item-8"></i> Toplam Ürün: <span class="badge badge-primary">@Model.Count</span></h5>
                            </div>
                            <div>
                                <a href="/Product/CreateProduct/" class="btn btn-primary">
                                    <i class="zmdi zmdi-plus"></i> Yeni Ürün Ekle
                                </a>
                            </div>
                        </div>

                        <div class="table-container">
                            <table class="table table-bordered table-hover">
                                <thead>
                                    <tr>
                                        <th><i class="zmdi zmdi-key"></i> ID</th>
                                        <th><i class="zmdi zmdi-label"></i> Ürün Adı</th>
                                        <th><i class="zmdi zmdi-money"></i> Birim Fiyat</th>
                                        <th><i class="zmdi zmdi-storage"></i> Stok</th>
                                        <th><i class="zmdi zmdi-view-list"></i> Kategori</th>
                                        <th><i class="zmdi zmdi-globe"></i> Menşei</th>
                                        <th><i class="zmdi zmdi-settings"></i> İşlemler</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <th>#@item.ProductId</th>
                                            <td><strong>@item.ProductName</strong></td>
                                            <td><strong class="text-success">@item.UnitPrice €</strong></td>
                                            <td><span class="badge badge-info">@item.StockQuantity</span></td>
                                            <td>@item.Category.CategoryName</td>
                                            <td>@item.CountryOfOrigin</td>
                                            <td>
                                                <div class="d-flex gap-2">
                                                    <a href="/Product/UpdateProduct/@item.ProductId" class="btn btn-success btn-sm" title="Güncelle">
                                                        <i class="zmdi zmdi-edit"></i>
                                                    </a>
                                                    <a href="/Product/DeleteProduct/@item.ProductId" class="btn btn-danger btn-sm" title="Sil" onclick="return confirm('Bu ürünü silmek istediğinize emin misiniz?')">
                                                        <i class="zmdi zmdi-delete"></i>
                                                    </a>
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>

                        <div class="mt-3 text-center">
                            <nav aria-label="Page navigation">
                                <ul class="pagination justify-content-center">

                                    @if (ViewBag.CurrentPage > 1)
                                    {
                                        <li class="page-item">
                                            <a class="page-link" href="/Product/ProductList?page=@(ViewBag.CurrentPage - 1)">Önceki</a>
                                        </li>
                                    }

                                    @{
                                        int startPage = Math.Max(1, ViewBag.CurrentPage - 2);
                                        int endPage = Math.Min(ViewBag.TotalPages, ViewBag.CurrentPage + 2);

                                        if (startPage > 1)
                                        {
                                            <li class="page-item">
                                                <a class="page-link" href="/Product/ProductList?page=1">1</a>
                                            </li>
                                            if (startPage > 2)
                                            {
                                                <li class="page-item disabled">
                                                    <span class="page-link">...</span>
                                                </li>
                                            }
                                        }

                                        for (int i = startPage; i <= endPage; i++)
                                        {
                                            <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                                                <a class="page-link" href="/Product/ProductList?page=@i">@i</a>
                                            </li>
                                        }

                                        if (endPage < ViewBag.TotalPages)
                                        {
                                            if (endPage < ViewBag.TotalPages - 1)
                                            {
                                                <li class="page-item disabled">
                                                    <span class="page-link">...</span>
                                                </li>
                                            }
                                            <li class="page-item">
                                                <a class="page-link" href="/Product/ProductList?page=@ViewBag.TotalPages">@ViewBag.TotalPages</a>
                                            </li>
                                        }
                                    }

                                    @if (ViewBag.CurrentPage < ViewBag.TotalPages)
                                    {
                                        <li class="page-item">
                                            <a class="page-link" href="/Product/ProductList?page=@(ViewBag.CurrentPage + 1)">Sonraki</a>
                                        </li>
                                    }

                                </ul>
                            </nav>
                        </div>


                        <div class="text-center mt-4">
                            <a href="/Product/CreateProduct/" class="btn btn-primary">Yeni Ürün Girişi</a>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
</section>


